package com.example.chat

import org.junit.jupiter.api.Disabled
import org.junit.jupiter.api.Test

class Slice11UiTest : AbstractIntegrationTest() {

    @Test
    @Disabled("Slice 11: implement the full React UI — Login page (/login), Register page (/register), Chat layout with sidebar (room list + friend list + presence indicators), Room view (message history + compose bar + member list), DM view, Settings page (sessions list, change password, delete account), Password Reset page (/reset-password?token=). Use react-router-dom v6 for routing. All API calls must include credentials: 'include' for cookie propagation. STOMP connection lives in a context provider at the app root.")
    fun `React UI renders all screens`() {
    }
}
